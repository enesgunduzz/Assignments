def counter(str):
        my_dictionary={}
        for i in str:
            if i in my_dictionary:
                my_dictionary[i] +=1
            else:
                my_dictionary[i] =1     
        return my_dictionary 
print(counter(str(input("give me a sentence: "))))
